package{	import data.*;	import scenes.*;	import game.data.*;	import game.errors.*;	import game.controllers.*;	/**	 * New Game	 * 	 * Author:		Billy Bateman	 * Date:		April 2012	 * Description:	New game setup.	 */	public class Main extends GameController	{		public function Main()		{						try {				this.construct();				super();			} catch ( error:Error ) {				GameError.setActionScriptError( error );			}		}		private function construct():void		{			if ( !GamePreloader.isActivated ) MainData.initMain( this );			MainData.initGame();						// Start a scene			//GameData.nextScene = MainScene.SCENE_NAME;		}	}}